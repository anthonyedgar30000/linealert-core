# LineAlert Core

Deterministic machine-event reasoning for LineAlert.

This repository is the implementation home for typed machine events, the Fusion Mosaic event fabric, temporal relationships, topology-aware drift interpretation, bounded diagnostic recommendations, PMV support, and governed promotion of new rule packs.

`main` is the trusted, reviewed baseline. Development happens on bounded branches and is merged through pull requests.
